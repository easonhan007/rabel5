$('#edit-modal').html("#{escape_javascript render('sort_planes', :planes => @planes, :formats => :html)}");
$('#edit-modal').modal('show');
-# rabel.sortable("#facebox .content", "#{sort_admin_planes_path}")
